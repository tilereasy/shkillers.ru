- name: Provision web servers
  hosts: webservers
  gather_facts: true
  become: true

  roles:
    - role: common
      tags:
        - common

    - role: security
      tags:
        - security
        
    - role: nginx
      tags:
        - nginx